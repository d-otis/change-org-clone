<%= render 'shared/notices' if flash.any? %>

<div class="row">
	<h1><%= @user.name %> : Dashboard</h1>
</div>
<div class="row">
	<div class="col-sm-3 mb-3">
		<h2>Account Info</h2>
		<ul class="list-group">
		  <li class="list-group-item">Email: <%= @user.email %></li>
		  <li class="list-group-item"><%= link_to "edit", edit_user_path(@user) %></li>
		</ul>
	</div>
	<div class="col-sm-9">
		<div class="row mb-3">
			<div class="col">
				<h2>My Petitions</h2>
				<div class="list-group">
					 <%= render partial: 'petitions/details', collection: @user.authored_petitions, :as => :petition %>
				</div>
			</div>
		</div>
		<div class="row">
			<div class="col">
				<h2>I've Supported...</h2>
				<div class="list-group">
					<%= render partial: 'signatures/details', collection: @user.signatures, :as => :signature %>
				</div>
			</div>
		</div>
	</div>

	
</div>
<%# console %>
