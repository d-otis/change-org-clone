<%= render 'shared/notices' if flash.any? %>
<div class="row mb-4">
	<div class="col text-center">
		<h2>Browse Petitions</h2>
	</div>
</div>

<%= render @petitions || "There are no Pettiions yet." %>

